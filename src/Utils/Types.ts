export type EmployeeType = {
  firstName: string
  lastName: string
  location: string
  prevSalary: string
  currSalary: string
  org: string
}
export type SalaryType = {
  salary: number
  location: string
  delta: number
}
